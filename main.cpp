
#include <stdlib.h>
#include <stdio.h>
//#include "lb_table.h"	
//#include "stat_table.h"	

int main(int argc, char *argv[]) {
/*
	lb_table *plb_tbl = new lb_table;
	lb_item item;
	lb_item *pitem;
	
	item.hash = 0x12345678;
	item.server.handle = 0;
	item.server.status = 0x111;
	plb_tbl->lb_new(&item);
	
	pitem = plb_tbl->lb_get(0x12345678);
	if (pitem == NULL) {
		printf("Can't find\n");
	} else {
		printf("Hash: 0x%lx, Status: 0x%x\n", pitem->hash, pitem->server.status);
	}
*/	
	
	printf("main done\n");
	return 0;
}

